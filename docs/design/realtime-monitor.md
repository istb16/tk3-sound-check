# 設計: 受動リアルタイムモニターの骨格

対象: ハウリングチェック / ボリュームチェック
状態: **未実装。実装する順番はハウリングが先。**

## なぜ今は実装しないのか

利用者が一人もいない抽象は、正しさを検証できない。いま骨格を書いても、それを使う
機能はゼロなので、合っているかどうかは誰にも分からない。半年後にハウリングを
書き始めた瞬間に「骨格が合っていない」と分かる、という最悪の順番になる。

したがってこの文書は「切り出すときの設計図」であって、先に作るべきコードの仕様ではない。
**ハウリングチェックを実装するときに、この文書を見ながら `src/lib/` へ切り出す。**

## 音質チェックと同じ形にしてはいけない理由

| | 音質チェック | ハウリング / ボリューム |
|---|---|---|
| 取り込み | 10秒で自動的に終わる | 終わりが無い。止めるまで聞き続ける |
| 状態 | `idle → recording → analyzing → done` | `idle → listening`（`done` が無い） |
| 出力 | 100点満点の採点と静的な結果画面 | 刻々変わる状態表示。点数という概念が無い |
| 終了時 | 結果が残る（保存・再生できる） | 何も残さない（履歴もサマリも作らない） |

同じ状態機械に乗せると両方が歪む。`AppState` を共有しないこと。

## 共通化するもの / しないもの

**する**

1. マイクの獲得と `AudioContext` のライフサイクル（`src/lib/audio.ts`）
2. 信号処理プリミティブ（`src/lib/signal.ts`）
3. シェル（ヘッダー・ルーティング・言語・`src/shell/app.css` のパネル）

**しない**

- 解析パイプライン（`analyzeSamples` の「バッファをもらってスコアを返す」形）
- 状態機械
- `VuMeter` / `RadarChart` / `ScoreBreakdown` / `VerdictPanel`（すべて音質チェック専用）

## 切り出す対象: `audio.ts` のストリーミング化

`setupRawCapture()` は既にワークレットからチャンクを main thread へ流し続けている。
いまは `parts[]` に溜めて最後に連結しているだけなので、**溜め込みをやめて都度
コールバックへ渡せば、そのままストリーミング解析になる。** 新規の仕掛けは要らない。

想定するインターフェース（実装時に確定させる）:

```ts
export interface MonitorHandle {
  /** マイクを閉じ、AudioContext を破棄する */
  stop: () => void;
  /** 生PCMを直接取れたか。false ならコーデック経由なので測定値は参考値 */
  rawCapture: boolean;
}

/**
 * マイクを開き、チャンクが届くたびに onChunk を呼ぶ。
 * サンプルレートは呼び出し側が必要とするので一緒に渡す。
 */
export function startMonitor(
  onChunk: (samples: Float32Array, sampleRate: number) => void,
): Promise<MonitorHandle>;
```

`recordMicrophone()` はこの上に実装し直せる（10秒ぶん溜めて止める）が、**移行は必須では
ない**。音質チェックは動いており、動いているものを触る理由が無い。骨格の検証は
新しい機能側で行う。

## 取り込み時の制約（全機能で共通・変更禁止）

```ts
{ echoCancellation: false, noiseSuppression: false, autoGainControl: false }
```

音質チェックではノイズフロアと音量を測るために必要だった。ハウリングでは
**より強い理由がある**——エコーキャンセラは、まさに検出したい発振をブラウザが
先に消してしまう。この3つがONの状態でハウリングチェックは成立しない。

## UI の共通形

```
[ 状態表示（大きく） ]      ← 安全 / 注意 / 発振中、または 小さすぎ / 適正 / 割れている
[ 実測値 ]                 ← 周波数、あるいは dBFS
[ 帯域バー or レベルバー ]  ← 100ms ごとに更新
[ 停止ボタン ]
```

停止したら `idle` に戻るだけで、結果画面には遷移しない。

## 更新レートとレイテンシ

見ながら操作する道具なので、レイテンシが製品価値そのものになる。

- FFT は既存の `FFT_SIZE = 2048`（44.1kHz で 21.5Hz 分解能・46ms）
- hop は 100ms。表示の更新間隔もこれに合わせる
- 判定は 500ms の移動窓（数フレームぶん）で行う

「表示用の短期履歴」と「終了時サマリ用の長期履歴」は別物である。前者は判定に必要なので
持つ。後者は作らない。
